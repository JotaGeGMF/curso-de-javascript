/*
DOM - document object model
é uma representação da página web que permite com que possa ser feita a manipulação do conteúdo e da estrutura do HTML.
Em resumo, é uma representação orientada a objetos da página web que pode ser alterada com scripts js. 

Árvore DOM - a página web pode ser representada em formato de uma árvore hierárquica, onde cada ramo é um nó/node (tags, textos, comentários).
Document: é o objeto raiz
*/